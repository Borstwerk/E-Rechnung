# BorstWerk – Integrationsauftrag für EInvoiceSender

Hinweis: Die weitere Übergabe der BorstWerk-Planung erfolgt außerhalb von GitHub als separates Hand-off-Paket. Diese Datei bleibt nur als historischer Planungsstand bestehen; an diesem Branch werden keine weiteren Planungsänderungen vorgenommen.
